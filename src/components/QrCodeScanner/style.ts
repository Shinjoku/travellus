import styled from "styled-components";

export const Container = styled.div`
  canvas,
  video {
    display: none;
    max-width: 100%;
  }

  .btn-actions {
    display: flex;
    justify-content: center;
    gap: 1rem;
  }
`;
